{
  "name": "functions",
  "description": "Kid Sequencer Cloud Functions",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "setup:stripe": "node scripts/setup-stripe-products.js",
    "check:stripe": "node scripts/check-stripe-config.js",
    "test": "node test-subscription-state.js"
  },
  "engines": { "node": "20" },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "stripe": "^16.0.0"
  },
  "private": true
}
